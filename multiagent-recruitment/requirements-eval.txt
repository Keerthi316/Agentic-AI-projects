# ============================================================
# Evaluation Framework Dependencies
# Multi-Agent Recruitment System
# ============================================================
# Install with:
#   pip install -r requirements-eval.txt
#
# These are SEPARATE from the main requirements.txt.
# The main application dependencies (langgraph, langchain, etc.)
# must also be installed for the evaluation framework to run.
# ============================================================

# ── Core Testing ────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-json-report==1.5.0       # Machine-readable JSON test results
pytest-timeout==2.3.1           # Per-test timeout enforcement

# ── DeepEval ────────────────────────────────────────────────
# LLM output quality evaluation (faithfulness, relevancy, etc.)
deepeval==1.4.5

# ── Giskard ─────────────────────────────────────────────────
# Agent vulnerability scanning (injection, bias, hallucination)
giskard==2.15.5

# ── Promptfoo ───────────────────────────────────────────────
# Promptfoo is a Node.js CLI tool; we generate its YAML config
# from Python and invoke it via subprocess.
# Install separately with: npm install -g promptfoo
# Python package for config generation only:
pyyaml==6.0.2

# ── Report Generation ───────────────────────────────────────
jinja2==3.1.4                   # HTML report templates
rich==13.7.1                    # Console output formatting
tabulate==0.9.0                 # Table formatting for text reports

# ── Data Validation ─────────────────────────────────────────
# Already in main requirements.txt but pinned here for isolation:
pydantic==2.7.4
pydantic-settings==2.3.4

# ── Utilities ───────────────────────────────────────────────
python-dotenv==1.0.1
typing-extensions==4.12.2
