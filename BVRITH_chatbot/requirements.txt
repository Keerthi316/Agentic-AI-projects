# ── Core RAG stack (existing — do not change) ────────────────────────────────
langchain==0.3.21
langchain-community==0.3.19
langchain-chroma>=0.2.4
langchain-openai==0.3.8
chromadb>=1.0.9
streamlit>=1.43.0
python-dotenv>=1.1.0
openai>=1.78.0
tiktoken>=0.9.0
pypdf>=5.4.0
docx2txt>=0.8
fpdf2>=2.8.2

# ── Observability (new) ──────────────────────────────────────────────────────
# tiktoken already above — used by llm_logger for token estimation

# ── AI Governance & Safety (new) ─────────────────────────────────────────────
# Giskard LLM scan (hallucination, prompt injection, bias, harmful content)
giskard>=2.15.0

# DeepEval evaluation metrics (hallucination, faithfulness, bias, toxicity)
deepeval>=1.0.0

# Promptfoo config generation (YAML writer)
# Note: promptfoo CLI itself is a Node.js tool — install separately:
#   npm install -g promptfoo
pyyaml>=6.0.2

# Fairness tests & governance report
pandas>=2.2.0

# ── Development & testing ────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
