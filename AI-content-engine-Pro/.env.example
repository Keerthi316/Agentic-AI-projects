# OpenRouter API key — powers text (gpt-4o-mini) and image (flux.2-pro) generation
# Get yours at https://openrouter.ai/keys
OPENAI_API_KEY=

# OpenRouter API key for video generation (wan-2.6 / sora-2)
# Can be a separate key or the same key as above
OPENROUTER_VIDEO_API_KEY=
